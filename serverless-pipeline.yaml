name: "On Push masters"

steps:
  - name: echo
    uses: '@serverless-cd/npm-publish'
    # if: ${{ contains(github.ref, 'engine')}}
    working-directory: ./code
    inputs:
      registry: //registry.npmjs.org
      token: ${{ secrets.npm_token }}
  