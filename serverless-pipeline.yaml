name: "On Push masters"

steps:
  - run: npm install @serverless-devs/s -g
  - run: s config get
  - plugin: "@serverless-cd/s-setup@0.0.3-beta.1"
    inputs:
      alias: "s-setup"
  - run: s config get
      